{
  "devDependencies": {
    "dotenv-cli": "^8.0.0"
  },
  "scripts": {
    "dev": "dotenv -- sam local start-api --parameter-overrides SupabaseUrl=$SUPABASE_URL SupabaseAnonKey=$SUPABASE_ANON_KEY SupabaseServiceRole=$SUPABASE_SERVICE_ROLE"
  }
}
